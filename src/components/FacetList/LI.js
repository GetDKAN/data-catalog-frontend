import styled from 'styled-components';

const LI = styled.li`
  background: #FFFFFF;
  padding: 0;
  list-style-type: none;
  width: 100%;
  position: relative;
  display: block;
  input[type=checkbox] {
    margin-right: 1rem;
  }
`;

export default LI;
